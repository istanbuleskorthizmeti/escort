/**
 * 🎯 HYDRA FLEET: SEARCH VERIFICATION TOKENS
 * Map of domain names to search console verification tokens (Google, Yandex, etc.)
 */
export const verificationMappings: Record<
  string,
  { google?: string; yandex?: string; bing?: string }
> = {
  "dorukcanay.digital": {
    yandex: "02b064639403aee7"
  }
};
