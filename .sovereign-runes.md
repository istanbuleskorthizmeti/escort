# 🛡️ DRKCNAY RUNES: GREY HACKER OPERATIONAL PROTOCOLS

This document serves as the permanent memory and ruleset for the DRKCNAY Elite SEO Infrastructure.

## 1. FOOTPRINT ANONYMIZATION
- **Rule**: Every build MUST randomize HTML class names and IDs using the `FootprintScrubber`.
- **Goal**: Prevent Google from identifying the 24+ domains as a single PBN (Private Blog Network).

## 2. SEMANTIC NOISE & HUMANIZATION
- **Rule**: Content MUST be hydrated with real-time "Human Noise" (district-specific weather, building names, temporal data).
- **Goal**: Bypass "Brainspam" and AI-content detection algorithms.

## 3. AGGRESSIVE NICHE DOMINATION
- **Rule**: Focus purely on direct "Escort" keywords. Avoid polite euphemisms. Use H1, H2, and Keyword Clouds for total saturation.
- **Goal**: Capture users searching for high-intent escort keywords.

## 4. CLOAKING INTEGRITY
- **Rule**: Bots and non-targeted traffic MUST only see the high-authority White Page (Gourmet Recipes).
- **Goal**: Maintain 100% safety and achieve #1 ranking for the shadow niche.

## 5. HYDRA ROTATION
- **Rule**: Social media posts MUST rotate between 100+ X accounts and use randomized delay intervals.
- **Goal**: Avoid platform-wide shadowbans and build long-term social authority.

## 6. DYNAMIC BITLY PREMIUM & SEO SLUGS (v12.0)
- **Rule**: Shortened links must target HOME pages only, using dynamic custom LSI slugs containing "escort" and target locations (e.g. `sisli-vip-escort-2026`).
- **Goal**: Feed high-intent URL semantics to Google bots while protecting Bitly Premium API quotas.

## 7. SHORTLINK DATABASE LOGGING (PRISMA POSTGRESQL)
- **Rule**: Every dynamically created short URL (Bitly, is.gd, clck.ru) must be persisted to the PostgreSQL `ShortLink` table via Prisma upserts.
- **Goal**: Maintain an immutable centralized link pool for future automated interlinking (Mesh cross-linking).

## 8. TELEGRAM SILENCE MODE & LEAD PRIORITY
- **Rule**: All automated background scripts (Blogger, Gist, Telegraph, Puppeteer Siege) MUST suppress Telegram alerts if `DISABLE_BACKLINK_TELEGRAM_NOTIFICATIONS="true"`.
- **Goal**: Prevent Telegram API rate limits and keep the channel focused strictly on high-priority customer Leads and direct slash-commands.

## 9. INFINITE METADATA SPINTAX SOVEREIGNTY
- **Rule**: Metadata generator engine MUST use deep-nested Spintax arrays (over 30+ Standard and 15+ Money site templates) to compute stable, seeded random titles and descriptions.
- **Goal**: Ensure 100% unique SEO titles and descriptions for every generated district and subdomain page, leaving no programmatic footprints.
